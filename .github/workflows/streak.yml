name: Daily Commit to Maintain Streak (Emoji Version)

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  auto-commit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Configure Git
        run: |
          git config user.name "Aqsaabbasi2690"
          git config user.email "aqsaabbasi2690@gmail.com"

      - name: Make daily commit with rotating emoji
        run: |
          # List of emojis (you can add/remove any)
          EMOJIS=("🔥" "✨" "🚀" "🌙" "💫" "🌟" "🎯" "💥" "⚡" "🌈" "🎉" "🔥" "📅" "📝" "🌸" "🩷" "🌿" "✨" "🫶" "🌙" "💗" "🍰" "🕊️" "🌼" "🎀" "🩵" "🌱" "🪽" "💐")

          # Pick emoji based on day of year
          INDEX=$(date +%j)
          EMOJI=${EMOJIS[$((INDEX % ${#EMOJIS[@]}))]}

          echo "Streak updated on $(date) $EMOJI" >> streak-log.txt
          git add streak-log.txt
          git commit -m "$EMOJI Auto-commit to keep streak alive"
          git push
